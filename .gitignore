# macOS
.DS_Store

# env files, in case any module folder adds one later
.env
.env.local
.env.*.local

# node
node_modules/
.next/
npm-debug.log*

# python
__pycache__/
*.pyc
venv/
.venv/

# browser automation scratch output
.playwright-mcp/

# Built embedding index: 13MB of vectors, regenerated with
# backend/build_index.py. Vercel gets it via .vercelignore not excluding it,
# but git does not need a binary-ish blob that changes whenever docs change.
module_3_Enterprise RAG/index/
