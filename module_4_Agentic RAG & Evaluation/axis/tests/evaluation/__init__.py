"""The evaluation slice — CLAUDE.md's gate between milestones.

Separate from `tests/unit` and `tests/acceptance` because it answers a different
question. Acceptance tests ask "does this criterion hold"; the evaluation slice
asks "is retrieval good enough to build the next milestone on".
"""
